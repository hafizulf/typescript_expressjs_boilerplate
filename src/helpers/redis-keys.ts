// export const getAccessTokenKey = (userId: string) => `auth:access-token:${userId}`;
export const getUserDataKey = (userId: string) => `user:data:${userId}`;
export const getRoleMenuPermissionsKey = (roleId: string) => `role:menu-permissions:${roleId}`;
